export default {
  fetch: jest.fn(),
  addEventListener: jest.fn(),
  useNetInfo: jest.fn(),
  configure: jest.fn(),
  refresh: jest.fn(),
  isConnected: {
    fetch: jest.fn(),
    addEventListener: jest.fn(),
  },
};
